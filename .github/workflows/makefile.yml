name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - run: sudo apt remove -y python3
    - name: Setup Python 2  # needed for old NaCl builds
      uses: actions/setup-python@v3.1.2
      with:
        python-version: 2.7
        cache: pip
    - run: pip2 install six
    - run: sudo apt install -y python-is-python2

    - name: Initialize env
      run: env/initialize.sh
    
    - name: Setup Python 3
      uses: actions/setup-python@v3.1.2
      with:
        python-version: 3.9
        cache: pip
      
    - name: Build
      run: source env/activate && make
